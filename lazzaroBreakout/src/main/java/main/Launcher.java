package main;

import gui.Navigator;
import gui.common.BaseScene;
import javafx.scene.Scene;

public class Launcher extends BaseScene {
    public Launcher(Navigator navigator) {
        super(navigator);
    }
}
